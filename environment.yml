name: consciousness
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - setuptools
  - wheel
  - pip:
      # Core scientific
      - numpy==1.26.4
      - scipy==1.16.3
      - pandas==2.3.3
      - scikit-learn==1.3.2
      
      # EEG processing
      - mne==1.11.0
      
      # Topological data analysis
      - ripser==0.6.12
      - persim==0.3.8
      - giotto-tda==0.6.2
      - giotto-ph==0.2.4
      
      # Complexity measures
      - antropy==0.1.9
      
      # Visualization
      - matplotlib==3.10.7
      - plotly==6.5.0
      
      # Jupyter
      - jupyter
      - jupyterlab
      - ipywidgets
      
      # Utilities
      - joblib==1.5.2
      - tqdm==4.67.1
      - pyyaml
      
      # Experiment (Necker cube)
      - pygame
